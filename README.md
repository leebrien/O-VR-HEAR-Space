This project utilizes Git LFS. Make sure to have Git LFS installed on your machine to successfully pull the project files.

If these LFS files aren't being downloaded. Run Git Bash on root directory - 'git lfs pull'
